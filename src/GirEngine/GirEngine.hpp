#pragma once

namespace pEngine::girEngine {
    // leaving this empty for now; it can hold global declarations or something
    // or maybe it can be the single-include file that you can use to get access to engine stuff
}
